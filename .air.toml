root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/jman-api ./cmd/jman-api"
  bin = "./tmp/jman-api"
  include_ext = ["go"]
  exclude_dir = ["bin", "tmp", "completions"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
